@page "/"
@using System.Runtime.CompilerServices
@using estudyRatings.Components.Models
@rendermode InteractiveServer

@inject LecturerService LecturerService

<PageTitle>🏠 Home</PageTitle>

<h1>🏠 Home</h1>

<!--
@if (Lecturers != null)
{
    <table>
        <thead>
            <tr>
                <th scope="col">Name</th>
                <th scope="col">Email address</th>
            </tr>
        </thead>
        <tbody>
        @foreach(var lecturer in Lecturers)
        {
            <tr>
                <td>@lecturer.Username</td>
                <td>@lecturer.EmailAddress</td>
            </tr>
        }
        </tbody>
    </table>
}
else
{
    <h2>none</h2>
}
-->

@code {
    private List<Lecturer> Lecturers;
    protected override async Task OnInitializedAsync()
    {
        await SearchLecturers();
    }
    private async Task SearchLecturers()
    {
        Lecturers = await LecturerService.GetLecturers();
    }
}
